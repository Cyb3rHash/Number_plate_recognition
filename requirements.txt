Flask
Gunicorn
# Use headless variant for servers (no GUI dependencies)
opencv-python-headless
pytesseract
requests
pymongo
Pillow
python-dotenv
numpy
# YOLOv8 by Ultralytics
ultralytics
# Optional acceleration/backends (installed only if needed in environment)
onnxruntime
